Rolls the Android client back to the last stable mirror-shell pairing flow.

Keeps the inline gallery action, preserves the visible sidebar create actions, and reverts the QR payload parsing changes that destabilized reconnect and fresh-install pairing.
